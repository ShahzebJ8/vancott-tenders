# Python
.venv/
__pycache__/
*.pyc

# Local run logs and reconnaissance dumps (large, and regenerable)
*.log
recon/

# Android / Gradle
.gradle/
build/
app/build/
local.properties
*.apk
*.keystore

# OS
Thumbs.db
.DS_Store

# Celias is a commercial font. It stays on our machines and in the APK we
# build, but it is never published to a public repository.
app/src/main/res/font/*.otf

# Build-time copy of the feed, regenerated by tools/bundle_data.py
app/src/main/assets/tenders.json
